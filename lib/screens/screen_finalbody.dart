import 'package:flutter/material.dart';

class FinalBodyScreen extends StatelessWidget {
  const FinalBodyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
